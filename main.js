//variaveis de controle dos slides
let slideShow = document.getElementsByClassName("slide");
let slideNum = 1;

//slideShow           
function currentSlides(slideNum) {
    return slideNum;
};